First iteration was pure JavaScript, HTML, and CSS, with Supabase auth and database. Gameplay was originally all done client side, so there was no safety and no official time verification.

Building new version, keeping Supabase but using React + TypeScript and Node.js for gameplay
